#get the datatype of an array containing strings

import numpy as np
arr=np.array(["apple","banana","cherry"])
print(arr.dtype)

#U:this indicates the datatype is unicode string
#6 : specifies the length of the unicode string in the array