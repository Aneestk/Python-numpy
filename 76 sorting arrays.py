#sorting arrays

#sorting means putting elements in a ordered sequance
#sort() that will sort a specified array

#sort thr array
import numpy as np
arr=np.array([3,2,0,1])
print(np.sort(arr))

#Note: this method returns a copy of array
#leaving the original array unchanged